import { aboutSchema } from './about';
import { categorySchema } from './category';
import { projectSchema } from './project';

export const schemaTypes = [categorySchema, projectSchema, aboutSchema];
